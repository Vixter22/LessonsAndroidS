<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_margin="5dp"
    android:background="#FFFFFF"
    android:padding="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- Зображення товару -->
        <ImageView
            android:id="@+id/imageViewOrderProduct"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher_background"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingStart="8dp">

            <!-- Назва товару -->
            <TextView
                android:id="@+id/textViewOrderProductName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Назва товару"
                android:textSize="16sp"
                android:textStyle="bold" />

            <!-- Ціна (розрахована як базова * кількість) -->
            <TextView
                android:id="@+id/textViewOrderProductPrice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Ціна: 0 грн"
                android:textColor="#FF0000"
                android:textSize="14sp"
                android:layout_marginTop="4dp"/>

            <!-- Відображення кількості -->
            <TextView
                android:id="@+id/textViewOrderQuantity"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Кількість: 1"
                android:textSize="16sp"
                android:layout_marginTop="4dp"/>
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
